BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Movie] ADD CONSTRAINT [Movie_isFirstTimeWatching_df] DEFAULT 1 FOR [isFirstTimeWatching];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
